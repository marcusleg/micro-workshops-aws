// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as CloudfrontS3LambdaStarterTemplate from '../lib/2024-03-01 CloudFront with S3 and Lambda Origin, starter template-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/2024-03-01 CloudFront with S3 and Lambda Origin, starter template-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new CloudfrontS3LambdaStarterTemplate.CloudfrontS3LambdaStarterTemplateStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
